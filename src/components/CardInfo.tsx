type CardInfoProps = {
    activity: string;
    type: string;
    price: number;
    participants: number;
  };
  
  const CardInfo = ({activity, type, price, participants}: CardInfoProps) => {
    return <></>;
  };
  
  export default CardInfo;